export class Alunos (
  nome: string;
  matricula: number;
  curso: string;
  notas: number [];
  constructor (nm; string, matri: number, curso: string; notas: number[]) {
  this.nome = nm;
  this.matricula = matri;
  this.curso = curso;
  this.notas = notas;
  }
  }
  